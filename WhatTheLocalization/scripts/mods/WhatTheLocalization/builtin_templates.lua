local mod = get_mod("WhatTheLocalization")

-- currently no built-in templates
local templates = {
	-- {
	-- 	id = "test_id",
	-- 	loc_keys = {
	-- 		"loc_example",
	-- 	},
	-- 	locales = {
	-- 		"zh-cn",
	-- 	},
	-- 	handle_func = function (locale, value, context)
	-- 		context.stacks = tostring(tonumber(context.stacks) + 1)
	-- 		return value
	-- 	end,
	-- },
}

return templates
